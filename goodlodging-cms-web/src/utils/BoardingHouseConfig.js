export const getArea=(minArea,maxArea)=>{
    return minArea===maxArea?minArea:minArea+" - "+maxArea;
}
export const getPrice=(minPrice,maxPrice)=>{
    return minPrice===maxPrice?minPrice:minPrice+" - "+maxPrice;

}